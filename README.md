# Sts Test

This project was built by using the latest version of Angular.

Clone this project by using `git clone https://github.com/Dimas-e63s/sts-test.git`

After that, run `npm i` in the terminal, in your local machine

And the last step - run `ng serve` in the terminal.

Navigate to `http://localhost:4200/`

Enjoy!)
